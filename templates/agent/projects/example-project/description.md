Example project — replace me. One paragraph: what this project is, where the
code lives, and what state it's in. This text is injected into the agent's
system prompt index, so the agent knows this project exists before you mention it.
