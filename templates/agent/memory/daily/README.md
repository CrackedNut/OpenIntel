# Daily Notes

One file per day: `YYYY-MM-DD.md`. The agent's working log — it reads today's
note before its first reply each session and appends `## HH:MM` checkpoints as
it works (decisions, gotchas, blockers, sign-off summaries). See DIRECTIVES.md.
